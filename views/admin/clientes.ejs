<%- include("../admin/partials/header") %>

<div class="container">

    <div class="topo">
        <h2>Clientes</h2>
        <a href="/admin/clientes/cadastrar">Adicionar Novo</a>
    </div>

    <div class="area-tabela">

        <table>

            <thead>

                <tr>
                    <th>ID</th>
                    <th>Nome</th>
                    <th>Email</th>
                    <th>Telefone</th>
                    <th>Situação Usuário</th>
                    <th>Ações</th>
                </tr>

            </thead>

            <tbody>

                <% for(let cliente of clientes) { %>

                    <tr>
                        <td><%= cliente.id %></td>
                        <td><%= cliente.nome %></td>
                        <td><%= cliente.email %></td>
                        <td><%= cliente.telefone %></td>
                        <td><%= cliente.situacao_usuario %></td>
                        <td>
                            <a href="/admin/clientes/editar/<%= cliente.id %>">Editar</a>
                            <a href="/admin/clientes/excluir/<%= cliente.id %>">Excluir</a>
                        </td>
                    </tr>

                <% } %>

            </tbody>

        </table>

    </div>

</div>