<%- include('./admin/partials/header') %>

<div class="container">

    <h1>Vendas</h1>

    <div>
        <h3><a href="/clientes/cadastrar">Adicionar Nova venda</a></h3>
    </div>

<div class="tabela-vendas">

    <table>

        <thead>
            <tr>
                <th>ID</th>
                <th>Cliente</th>
                <th>Status</th>
                <th>Data de Compra</th>
                <th>Ultima Modificação</th>
                <th>Ações</th>
            </tr>
        </thead>

        <tbody>

            <% for (let venda in vendas) { %>

                <tr>
                    <td><%= venda.id %></td>
                    <td><%= venda.cliente %></td>
                    <td><%= venda.status %></td>
                    <td><%= venda.dataCompra %></td>
                    <td><%= venda.dataModificacao %></td>
                    <td>
                        <a href="/admin/vendas/editar">Visualizar</a>
                        <a href="/admin/vendas/editar">Editar</a>
                    </td>
                </tr>

            <% } %>

        </tbody>

    </table>

</div>

</div>